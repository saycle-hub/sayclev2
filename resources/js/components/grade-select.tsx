import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
export function GradeSelect({ value, onChange, id, ariaLabelledBy }: { value?: string; onChange: (value: string) => void; id?: string; ariaLabelledBy?: string }) { return <Select value={value} onValueChange={onChange}><SelectTrigger id={id} aria-labelledby={ariaLabelledBy} className="h-12 rounded-lg border-[#c2c8be] bg-white text-[#191c1c] shadow-none focus:border-[#1d3a20] focus:bg-white focus:ring-[#1d3a20]/20"><SelectValue placeholder="Pilih grade" /></SelectTrigger><SelectContent>{[['Layak', 'Pakan ternak'], ['Kurang Layak', 'Maggot'], ['Tidak Layak', 'Kompos']].map(([g, use]) => <SelectItem key={g} value={g}>{g} — {use}</SelectItem>)}</SelectContent></Select>; }
