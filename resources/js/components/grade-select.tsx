import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
export function GradeSelect({ value, onChange, id, ariaLabelledBy }: { value?: string; onChange: (value: string) => void; id?: string; ariaLabelledBy?: string }) { return <Select value={value} onValueChange={onChange}><SelectTrigger id={id} aria-labelledby={ariaLabelledBy} className="focus:border-[#e88c12] focus:ring-[#e88c12]/30"><SelectValue placeholder="Pilih grade" /></SelectTrigger><SelectContent>{[['Layak', 'Pakan ternak'], ['Kurang Layak', 'Maggot'], ['Tidak Layak', 'Kompos']].map(([g, use]) => <SelectItem key={g} value={g}>{g} — {use}</SelectItem>)}</SelectContent></Select>; }
