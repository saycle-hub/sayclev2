import { Head, useForm } from '@inertiajs/react';
import { KeyRound, Search } from 'lucide-react';
import { FormEvent } from 'react';
export default function Track() { const form = useForm({ sale_id: '', pin: '' }); const submit = (e: FormEvent) => { e.preventDefault(); form.post(route('tracking.show')); }; return <><Head title="Lacak pickup — SayCle" /><main className="grid min-h-screen place-items-center bg-white px-5 text-[#18352a]"><section className="w-full max-w-md"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#e88c12]">SayCle · Tracking aman</p><h1 className="mt-3 text-5xl font-semibold tracking-[-.05em]">Lacak laporanmu.</h1><p className="mt-5 leading-7 text-[#18352a]/65">Masukkan Sale ID dan PIN. ID saja tidak cukup untuk membuka status.</p><form onSubmit={submit} className="mt-9 space-y-4 rounded-[2rem] bg-white p-7 shadow-xl shadow-[#18352a]/10"><label className="block text-sm font-semibold">Sale ID<input required className="mt-2 min-h-12 w-full rounded-xl border border-[#18352a]/15 bg-white px-4" value={form.data.sale_id} onChange={e => form.setData('sale_id', e.target.value)} /></label><label className="block text-sm font-semibold"><KeyRound size={16} className="mr-2 inline" />PIN<input required inputMode="numeric" className="mt-2 min-h-12 w-full rounded-xl border border-[#18352a]/15 bg-white px-4 tracking-[.2em]" value={form.data.pin} onChange={e => form.setData('pin', e.target.value)} /></label><button disabled={form.processing} className="flex min-h-12 w-full items-center justify-center rounded-full bg-[#e88c12] font-bold">{form.processing ? 'Memeriksa…' : <><Search size={18} className="mr-2" />Lihat status</>}</button></form></section></main></>; }
